/**
 * Placeholder for Supabase generated Database types.
 *
 * When the project is linked and migrations are applied, generate with:
 *   npx supabase gen types typescript --linked > types/database.ts
 * (or equivalent CLI flags for your setup)
 *
 * Do not hand-write a large fake Database interface here.
 */

export type Database = Record<string, never>;
